import { View, Text } from "react-native";
import React from "react";

const useLoginScreen = () => {
  return (
    <View>
      <Text>useLoginScreen</Text>
    </View>
  );
};

export default useLoginScreen;
